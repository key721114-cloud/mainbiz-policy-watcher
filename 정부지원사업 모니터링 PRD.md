# 정부지원사업 모니터링 PRD

2026-09-18 · @Someone

## 목적 및 배경

메인비즈협회는 정책사업팀 차원에서 나라장터·기업마당 API를 활용해 특정 키워드가 포함된 지원사업 공고를 구글시트로 자동 수집하는 프로그램(esg\_watcher)을 운영해왔다. 그러나 2027년 예산안 100대 신규사업 검토 및 정부 오픈API 실태 조사 과정에서, 기존 수집 체계가 협회가 실제로 놓치지 말아야 할 공고의 상당 부분(특히 산업통상부 및 산하기관이 자체 예산으로 운영하는 공고)을 구조적으로 포착하지 못한다는 사실이 확인되었다.

이 PRD는 esg\_watcher를 고도화하여, "협회가 눈여겨보는 공고가 뜨면 실시간으로 한 번에 모아오는" 원래 목적을 부처·산하기관 단위까지 실질적으로 달성하기 위한 개발 방향을 정의한다.

## 현황 진단: 기존 수집 체계의 한계

- 기존 수집원(나라장터, 기업마당)은 각각 구조적 한계가 있다.
- 나라장터는 지원사업 '모집 공고'가 아니라, 이미 선정된 보조사업자가 지원금을 집행(장비·용역 구매)하는 '조달 계약' 단계에서 뜨는 정보다. 모집 공고보다 시점이 늘고(수개월 뒤), 순수 출연금·보조금 사업은 애초에 나라장터를 거치지 않는 경우도 많아 커버리지가 낮다.
- 기업마당은 SIMS(중소기업 지원사업 통합관리시스템)에 각 사업 수행기관 담당자가 수기로 입력해야 노출되는 구조다. 등록이 법적으로 강제되지 않는 경우가 많아, 특히 산하기관 자체 예산 사업은 등록 자체가 누락될 가능성이 있다. 즉 "기업마당만 보면 다 잡힌다"는 전제 자체가 성립하지 않는다.
- 오픈API 실태 조사 결과, 사업공고를 실시간 API(openapi.do)로 제공하는 곳은 중기부 계열(기업마당, 중기부 사업공고, K-Startup, TIPA 중소벤처24) 4곳과, 조달청 나라장터(현재 연동 중이나 집행 단계 정보라 모집공고 조기포착용으로는 보조적)뿐이다. 산업통상부는 부처 본체부터 산하기관(KIAT·KEIT·KOTRA·KETEP)까지 사업공고 API가 전무하며, 중기부 산하기관(소진공·KOSMES)도 사업공고 API는 없다(소진공은 상권정보 API만, KOSMES는 자체 포털은 있으나 등록된 API 없음).

## 목표

1. 현재 보유한 API 5종(중기부, 중소벤처24, 창업진흥원, 나라장터, 기업마당)을 안정적으로 통합 수집한다 (즉시 실행 가능).
2. API가 없는 산업부 본체 및 산하기관(KIAT·KEIT·KOTRA 등)의 공고를 크롤링/모니터링으로 보완한다.
3. API 트랙과 크롤링 트랙을 하나의 통합 수집 파이프라인 안에서 운영하여, 소스가 늘어나도 확장 가능한 구조를 만든다.
4. 키워드 매칭 대상을 100대 신규사업 검토를 통해 확보한 제조AI·피지컬AI·재도전(사업전환)· 5극 3특 성장엔진 등으로 확대한다.
5. 최종적으로 "협회가 눈여겨보는 공고가 뜨면 실시간으로 한 번에 모은다"는 원 목적에 맞게, 팀원 누구나 웹 대시보드 하나로 신규 공고를 실시간(또는 준실시간)으로 확인할 수 있는 체계로 전환한다.

## 수집 대상 범위

**중소벤처기업부 계열**

- 중소벤처기업부(본체) — API 있음
- 기업마당(bizinfo.go.kr) — API 있음
- 창업진흥원(K-Startup) — API 있음
- 중소기업기술정보진흥원(TIPA, 중소벤처24 공고정보) — API 있음
- 중소벤처기업진흥공단(KOSMES) — API 없음 (자체 포털 있으나 등록 API 없음)
- 소상공인시장진흥공단(소진공) — API 없음 (상권정보 API만 존재)

**산업통상부 계열**

- 산업통상부(본체) — API 없음, 홈페이지 게시판만 존재
- 한국산업기술진흥원(KIAT) — API 없음, 게시판은 JS 렌더링 구조
- 한국산업기술기획평가원(KEIT) — API 없음, 정적 파일데이터만 존재(실시간성 낮음)
- 대한무역투자진흥공사(KOTRA) — API 없음, 게시판은 JS 렌더링 구조
- 한국에너지기술평가원(KETEP) — API 없음, 이번 조사에서 사이트 구조 미확인(접근 제약)

**제외 대상**

- 과학기술정보통신부: 자체 사업공고 API가 존재하나, 지원영역이 협회 회원사(중소 제조업 중심)와 정합성이 낮아 1차 범위에서 제외한다.

## 소스별 현황 및 수집 전략

| 소스 | 소관 | 수집방식 | 구현 우선순위 | 비고 |
| --- | --- | --- | --- | --- |
| 기업마당 | 중기부 | API | 1순위 | 즉시 연동 가능 |
| 중기부 사업공고 | 중기부 | API | 1순위 | 즉시 연동 가능 |
| K-Startup | 중기부(창업진흥원) | API | 1순위 | 즉시 연동 가능 |
| TIPA 중소벤처24 공고정보 | 중기부(TIPA) | API | 1순위 | 즉시 연동 가능 |
| KOSMES | 중기부 | 크롤링(대상 목록 포함) | 크롤링 대상 | 자체 포털 구조 재확인 필요 |
| 소진공 | 중기부 | 크롤링(대상 목록 포함) | 크롤링 대상 | 사이트 구조 미확인(접근 제약) |
| 산업통상부 본체 | 산업부 | 크롤링(대상 목록 포함) | 크롤링 대상 | 정적 게시판으로 추정 |
| KIAT | 산업부 | 크롤링(대상 목록 포함, 헤드리스 브라우저) | 크롤링 대상 | JS 렌더링, 개발비용 높음 |
| KOTRA | 산업부 | 크롤링(대상 목록 포함, 헤드리스 브라우저) | 크롤링 대상 | JS 렌더링, 개발비용 높음 |
| KEIT | 산업부 | 크롤링(대상 목록 포함) | 크롤링 대상 | data.go.kr은 정적파일뿐 |
| KETEP | 산업부 | 크롤링(대상 목록 포함) | 크롤링 대상 | 사이트 구조 미확인(접근 제약) |

나라장터(조달청) API는 현재 이미 연동되어 있어 1단계에 함께 포함하려 느, 마네 모집공고가 아니라 지원금 집행(조달 계약) 단계의 정보라 신규 공고 조기포착용으로는 쓰지 않고, 선정된 사업의 집행 현황을 후속 추적하는 보조 용도로 사용한다.

KOSMES·소진공·산업통상부 본체·KIAT·KOTRA·KEIT·KETEP은 모두 '크롤링 대상 목록'에 동일하게 포함되며, 순위를 매기지 않고 목록에 있는 모든 사이트를 순차 조사한다.

## 시스템 아키텍처: API 트랙 + 크롤링 트랙 통합 구조

하나의 수집 파이프라인 안에 소스별 '어댑터' 모듈을 두는 구조로 설계한다.

- **API 어댑터**: 기업마당·중기부 사업공고·K-Startup·TIPA 각각의 오픈API를 호출해 JSON/XML 응답을 파싱한다. 호출 비용이 낮아 하루 여러 차례 실행 가능하다.
- **크롤링 어댑터**: 기관별로 하나씩 존재하며, 정적 HTML 사이트는 가벼운 파서(requests+BeautifulSoup 등)로, JS 렌더링 사이트(KIAT·KOTRA)는 헤드리스 브라우저(Playwright)로 목록을 가져온다. 상대 서버 부담을 고려해 하루 1회 수준으로 제한한다.
- 모든 어댑터는 공통 출력 스키마(사업명·소관기관·접수기간·원문링크·수집일시)로 결과를 반환하며, 하나의 오케스트레이터가 소스를 순차 호출해 결과를 취합한다.
- 개별 어댑터의 실패(사이트 구조 변경, 접속 차단 등)이 전체 파이프라인을 중단시키지 않도록 소스별로 예외 처리를 분리한다.
- 실행 환경은 협회의 기존 Google Apps Script 자동화 자산을 최대한 활용하되, 헤드리스 브라우저가 필요한 크롤러는 Apps Script 환경 밖의 GitHub Actions에서 별도 실행한다. 수집 결과(JSON)를 같은 레포지토리에 커밋하면, 그 레포와 연동된 Vercel이 push를 감지해 자동 재배포하며 웹 대시보드를 최신 상태로 유지한다(구체 구현은 개발 착수 시 별도 논의).
- 키워드 목록과 크롤링 대상 목록은 코드에 하드코딩하지 않고 별도 설정 파일(`keywords.env`, `crawl_targets.csv`)로 분리해, 학준님이 직접 등록·수정할 수 있도록 한다. `keywords.env`는 콤마로 구분된 키워드 목록을, `crawl_targets.csv`는 기관명·목록URL·렌더링방식·확인상태를 담는다.
- **웹 대시보드 어댑터**: 수집 결과를 JSON으로 레포지토리에 커밋하면, 연동된 Vercel이 push를 감지해 자동 재배포한다. 별도 백엔드 서버나 이메일 발송 계정 없이, 정적 사이트(index.html + JSON fetch) 만으로 동작한다. 팀원들은 별도 알림 없이 이 대시보드 URL에 직접 접속해 최신 공고 목록을 확인한다.

## 데이터 스키마 및 중복 처리

| 필드 | 설명 |
| --- | --- |
| 공고명 | 원문 제목 |
| 소관·수행기관 | 부처/산하기관명 |
| 수집 방식 | API / 크롤링 |
| 접수기간 | 시작일\~마감일(확인 가능한 경우) |
| 원문 링크 | 공고 상세 페이지 URL |
| 키워드 매칭 | 매칭된 키워드 목록 |
| 수집일시 | 시스템이 최초 수집한 시각 |
| 중복 여부 | 타 소스와 중복 수집된 경우 표시 |

중복 판별은 '공고명 유사도 + 원문 링크' 기준으로 수행한다. 기업마당과 TIPA(중소벤처24)처럼 소스가 격칠 가능성이 있는 채널은 초기에는 모두 수집하되 중복 제거 로직으로 거르고, 완전히 격친다고 판단되면 이후 단계에서 소스를 정리한다.

## 운영 방식

- **실행 주기**: API 어댑터는 1일 2\~3회, 크롤링 어댑터는 1일 1회를 기본값으로 한다.
- **알림**: 신규 공고(중복 아닌 건)을 포함한 수집 결과를 매 실행마다 JSON 파일로 정리해 레포지토리에 커밋한다. 이메일 등 별도 푸시 알림은 보내지 않고, 팀원들은 Vercel에 배포된 웹 대시보드 URL에 접속해 최신 공고 목록을 확인하는 방식으로 전환한다.
- **모니터링**: 각 어댑터의 마지막 성공 실행 시각을 기록하여, 특정 소스가 일정 기간 이상 수집 실패 상태이면 담당자에게 별도로 알린다(사이트 구조 변경 감지 대용).

## 단계별 로드맵

**1단계 (즉시 착수)**: 기존 보유 API 5종(중기부·중소벤처24·창업진흥원·나라장터·기업마당) 연동 안정화. 키워드 목록 1차 확정(제조AI·피지컬AI·재도전· 5극 3특 성장엔진 등 반영).

**2단계**: API가 없는 모든 기관(산업통상부 본체, KIAT, KOTRA, KEIT, KETEP, 소진공, KOSMES)을 크롤링 대상 목록으로 명문화하고, 우선순위를 두지 않고 목록의 모든 사이트를 순차적으로 조사한다(정적/JS 렌더링 여부, 접근 가능 여부 확인). 이번 조사에서 접근이 막혔던 KETEP·소진공도 이 단계에서 함께 재확인한다.

**3단계**: 2단계에서 조사된 결과를 바탕으로 사이트별 크롤러를 개발한다. 정적 구조 사이트부터 순차적으로 개발하되, JS 렌더링 사이트(KIAT·KOTRA)는 헤드리스 브라우저 구현이 필요해 개발 기간이 더 소요될 수 있음을 일정에 반영한다. 목록에 있는 기관은 순서에 관계없이 모두 개발 대상에 포함된다.

**4단계**: 전체 소스 통합 후 알림 체계를 실시간화하고, 중복 제거·키워드 매칭 로직을 운영 데이터 기반으로 튜닝.

## 리스크 및 제약사항

- **개발 리스크**: 헤드리스 브라우저 기반 크롤러는 기존 Apps Script 자동화 방식보다 개발·운영 난이도가 높고, 별도 실행 환경(서버/CI)이 필요하다.
- **유지보수 리스크**: 크롤링 대상 사이트가 구조를 변경하면 수집이 조용히 실패할 수 있어, API 대비 유지보수 빈도가 잦다.
- **접근 제약**: 이번 조사 과정에서 일부 기관 사이트(KETEP, 소진공)는 네트워크/보안 정책상 접근이 제한되어 구조를 확정하지 못했다. 실제 개발 착수 전 재확인이 필요하다.
- **법적/윤리적 고려**: 크롤링 대상 사이트의 이용약관 및 접근 제한(robots.txt 등)을 사전에 확인하고, 과도한 요청으로 상대 서버에 부담을 주지 않도록 실행 주기를 보수적으로 설정한다.
- **커버리지 한계**: 위 조치를 모두 이행해도, 공고 자체가 어느 채널에도 게시되지 않는 경우(내부 공문 등)는 원천적으로 수집 범위 밖이다.

## 향후 과제

- 키워드 목록 최종안은 협회 내부 사전검토 후 별도로 확정한다(기존 계획대로).
- 크롤링 대상 산하기관(KIAT·KOTRA 등)의 실제 게시판 구조는 담당자가 직접 브라우저로 재확인하여 개발 착수 전 스펙을 보완한다.
- 산업부 산하기관 외 지자체·기타 유관기관 공고 채널 확대 여부는 1\~3단계 운영 성과를 보고 별도 검토한다.
